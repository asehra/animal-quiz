require 'spec_helper'

describe AnimalQuiz do
  it 'has a version number' do
    expect(AnimalQuiz::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
